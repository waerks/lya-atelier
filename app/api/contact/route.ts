import {NextResponse} from 'next/server'; export async function POST(req:Request){try{const {name,email,message}=await req.json();if(!name||!email||!message||!/^\S+@\S+\.\S+$/.test(email))return NextResponse.json({error:'Champs invalides'},{status:400}); console.log('[LYA CONTACT]',{name,email,message,receivedAt:new Date().toISOString()}); return NextResponse.json({ok:true,mode:'local'});}catch{return NextResponse.json({error:'Requête invalide'},{status:400})}}
