import {getProjects} from '@/lib/projects'; import ProjectList from '@/components/ProjectList'; import SectionTitle from '@/components/SectionTitle'; export const metadata={title:'Archives'}; export default function Archives(){const years=['2028-2029','2027-2028','2026-2027'] as const; return <main className="mx-auto max-w-7xl px-6 pb-20 pt-40 md:px-10"><SectionTitle eyebrow="Le temps du projet" title="Archives"/><div className="space-y-28">{years.map((year,i)=><section key={year}><div className="mb-8 flex items-baseline gap-5 border-b border-brown/20 pb-4"><h2 className="font-display text-4xl">{year}</h2><span className="text-[10px] uppercase tracking-widest text-gray">Année {3-i}</span></div><ProjectList projects={getProjects().filter(p=>p.schoolYear===year)}/></section>)}</div></main>}
