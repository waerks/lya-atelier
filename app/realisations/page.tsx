import {getProjects} from '@/lib/projects'; import ProjectList from '@/components/ProjectList'; import SectionTitle from '@/components/SectionTitle'; export const metadata={title:'Réalisations'}; export default function Realisations(){return <main className="mx-auto max-w-7xl px-6 pb-20 pt-40 md:px-10"><SectionTitle eyebrow="Projets en cours et livrés" title="Réalisations"/><p className="mb-16 max-w-md text-gray">Une sélection de lieux transformés avec attention, de Paris à Bruxelles.</p><ProjectList projects={getProjects().filter(p=>p.status==='réalisation').sort((a,b)=>b.schoolYear.localeCompare(a.schoolYear))}/></main>}
