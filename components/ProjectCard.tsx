import Link from 'next/link'; import type {Project} from '@/lib/projects'; export default function ProjectCard({project,index=0}:{project:Project;index?:number}){return <Link href={`/projets/${project.slug}`} className="group block animate-fade-up" style={{animationDelay:`${index*80}ms`}}><div className="visual aspect-[4/3]" style={{'--c1':project.gallery[0],'--c2':project.gallery[1]} as React.CSSProperties}><span className="absolute bottom-5 left-5 z-10 max-w-[70%] font-display text-3xl text-ivory md:text-4xl">{project.title}</span><span className="absolute right-5 top-5 z-10 text-[10px] uppercase tracking-widest text-ivory/80">{String(index+1).padStart(2,'0')}</span></div><div className="flex justify-between pt-3 text-[10px] uppercase tracking-[.16em] text-gray"><span>{project.schoolYear.slice(0,4)} — {project.category}</span><span>{project.location}</span></div></Link>}
